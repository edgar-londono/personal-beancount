; ============================================================
; main.bean — Entry point
;
; This file does nothing but stitch the other ledger files
; together, in the order Beancount should load them.
;
; File organization and comment-separator rules live in rules.md,
; sections 1 and 2.
; ============================================================

; ============================================================
; main.bean — Config
; ============================================================

option "operating_currency" "COP"

; ============================================================
; accounts.bean — Chart of accounts
; ============================================================

include "accounts.bean"

; ============================================================
; prices.bean — Foreign currency / investment prices
; ============================================================

include "prices.bean"

; ============================================================
; [YEAR].bean — Transactions for a given year
; ============================================================

; --- 202X ---
include "2026.bean"